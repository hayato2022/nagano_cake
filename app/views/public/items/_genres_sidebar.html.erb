<div class="col-md-2 mt-5 mr-4 ">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>ジャンル検索</th>
          </tr>
          <tbody class="table table-borderless">
            <% genres.each do |genre| %>
            <tr>
              <td>
                <%= link_to items_path(genre_id: genre.id), class: "text-dark" do %>
                  <%= genre.name %>
                <% end %>
              </td>
            </tr>
            <% end %>
          </tbody>
        </thead>
      </table>
    </div>
